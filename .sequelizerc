require('dotenv').config();
const path = require('path');

module.exports = {
  env: process.env.NODE_ENV,
  config: path.resolve('src', 'core', 'database', 'config.js'),
  'models-path': path.resolve('src', 'core', 'database', 'models'),
  'seeders-path': path.resolve('src', 'core', 'database', 'seeders'),
  'migrations-path': path.resolve('src', 'core', 'database', 'migrations'),
};
